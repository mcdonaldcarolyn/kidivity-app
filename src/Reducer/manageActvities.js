
export const initialState = {
    activites: []
};

export default function manageActivites(state= initialState, action){
    {
        switch(action.type){
            
        }
        return state
    }
}
